<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  
  <body>
    <div class="flex flex-col h-full min-h-screen items-center justify-start">
   <div class="flex flex-col w-full">
  <header class="flex flex-row justify-between items-center bg-blue-900 px-6 py-5">
    <div class="header-logo font-bold text-white underline">
        <%= button_to "Log out", destroy_user_session_path, data: { turbo_method: :delete } %>
    </div>

    <div class="flex flex-row gap-2 py-5 text-white font-light">
        <div class="hover:font-bold">
            <a href="/">Home</a>
        </div>
        <div class="hover:font-bold">
            <a href=<%= books_path%>>Books</a>
        </div>
        <div class="hover:font-bold">
            <a href=<%= authors_path%>>Authors</a>
        </div>
    </div>
 </header>
  </div>
    <div class="flex grow w-full">
    <%= yield %>
    </div>
  </div>
  </div>
  </body>
</html>
